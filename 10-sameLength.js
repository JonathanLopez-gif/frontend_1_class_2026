let text1 = `JavaScript`
let text2 = `Java`

// Manera básica

console.log(text1.length === text2.length)

// Guardando el resultado en una variable

let sameLength = text1.length === text2.length

console.log(sameLength)
